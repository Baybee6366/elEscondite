import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  mainContiner: {
    flex: 1,
    backgroundColor: '#fff',
    },
    textColor: {
        color: '#000',
    }
});

export default style;